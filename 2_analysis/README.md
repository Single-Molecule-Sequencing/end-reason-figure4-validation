# 2_analysis

- `scripts/assemble_controlled_validation_composite.py`: deterministic panel compositor (PyMuPDF).
- `commands.md`: exact command lines used for assembly and derivative export.
- `environment.yaml`: minimal environment spec to execute the compositor and exports.

This stage reconstructs the controlled-validation composite from documented A/B/C source artwork and panel-C data-derived output.
